# Copy to .env and fill in your keys: cp .env.example .env

# Required — used by the shipped agent config (config.py REGISTRY):
ANTHROPIC_API_KEY=    # planner, researcher, verifier, synthesizer
OPENAI_API_KEY=       # extractor
TAVILY_API_KEY=       # web search
